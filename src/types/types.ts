export type TProduct = {
  id: number;
  image: string;
  title: string;
  brand: string;
  price: number;
  description: string;
};

export type TCountry = {
  name: string;
  label: string;
};
